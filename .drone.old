kind: pipeline
name: test-on-amd64

platform:
  arch: amd64

steps:
- name: test
  image: gcc
  commands:
  - git clone https://github.com/Kitware/CMake.git
  - cd CMake
  - ./configure
  - make && make install
  - cd ..
  - cmake .
  - make
  - ./wigner 
